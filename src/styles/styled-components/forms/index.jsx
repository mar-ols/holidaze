import { Modal } from "react-bootstrap";
import styled from "styled-components";

const StyledModal = styled(Modal)`
  .modal-title {
    text-align: center;
  }

  .modal-body {
    margin-bottom: 1rem;
  }
`;

export { StyledModal };
